<% content_for :sidebar do %>
  <ul class="nav nav-list">
    <li class="nav-header"><%= t('menu') %></li>
    <li><%= link_to t('menu_bands'), bands_path %></li>
    <% if user_signed_in? && current_user.roles_list.include?(User::BAND_MEMBER) %>
      <li><%= link_to t('menu_my_bands'), band_participations_path %></li>
    <% end %>
  </ul>
<% end %>