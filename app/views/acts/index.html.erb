<h1>Listing acts</h1>

<table>
  <tr>
    <th>Gig</th>
    <th>Band</th>
    <th>Is main act</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @acts.each do |act| %>
  <tr>
    <td><%= act.gig_id %></td>
    <td><%= act.band_id %></td>
    <td><%= act.is_main_act %></td>
    <td><%= link_to 'Show', act %></td>
    <td><%= link_to 'Edit', edit_act_path(act) %></td>
    <td><%= link_to 'Destroy', act, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Act', new_act_path %>
